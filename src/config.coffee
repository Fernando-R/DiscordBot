#this contais informations that are used in the bot, you may want to keep this hide
config = {
    "token": "",
    "prefix": "?",
    "commands": [
      'test'
    ]
}
#export the content
module.exports = { config };
